import {StyleSheet, View} from "react-native"

const Separador = ()=> <View style={styles.separadorMenu} />

const styles = StyleSheet.create({
    separadorMenu: {
        height: 15,
    }
})

export default Separador